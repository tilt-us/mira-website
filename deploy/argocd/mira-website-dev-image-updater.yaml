apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: mira-website-dev
  namespace: argocd
spec:
  writeBackConfig:
    method: argocd
  applicationRefs:
    - namePattern: mira-website-dev
      images:
        - alias: mirawebsite
          imageName: ghcr.io/tilt-us/mira-website:development
          commonUpdateSettings:
            updateStrategy: digest
            platforms:
              - linux/amd64
          manifestTargets:
            kustomize:
              name: ghcr.io/tilt-us/mira-website
