apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mira-website
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - staging.tilt-us.com
      secretName: mira-website-staging-tls
  rules:
    - host: staging.tilt-us.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mira-website
                port:
                  name: http
